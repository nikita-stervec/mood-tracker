import { HeaderComponent } from '@/widgets/header';
import { Tracker } from '@/widgets/tracker';

export const TrackPage = () => {
  return (
    <div>
      <HeaderComponent />
      <Tracker />
    </div>
  );
};
